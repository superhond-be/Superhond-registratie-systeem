# === Publieke URL van je site (optioneel; de function gebruikt dit als fallback om /data/agenda.json te fetchen)
PUBLIC_BASE_URL=https://jouw-site-naam.netlify.app

# === CORS (mag * voor test; in productie liever je eigen domein)
CORS_ALLOW_ORIGIN=*

# === Cache TTL voor get-agenda (seconden)
CACHE_TTL_SECONDS=60

# === Supabase (EU project)
# URL vind je in Supabase project settings → API
SUPABASE_URL=https://xxxxxxx.supabase.co

# Service Role Key (alleen in functions/servers gebruiken! NIET in frontend)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# (optioneel) Anon key — alleen nodig als je Supabase rechtstreeks in de frontend gebruikt
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
